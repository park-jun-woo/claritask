# Claribot Web UI 기획안

## 1. 개요

### 1.1 목적
Claribot의 모든 기능을 브라우저에서 시각적으로 관리하는 웹 대시보드. 텔레그램과 CLI로만 가능했던 조작을 직관적인 UI로 확장한다.

### 1.2 핵심 가치
- **시각화**: Task 트리, 의존성 그래프, 스케줄 타임라인을 눈으로 확인
- **실시간**: Claude 실행 상태, 로그 스트리밍을 라이브로 모니터링
- **편의성**: 드래그앤드롭으로 Task 관리, 인라인 편집, 원클릭 실행

### 1.3 기술 스택
| 구분 | 선택 | 이유 |
|------|------|------|
| **프레임워크** | React + TypeScript | 컴포넌트 기반, 타입 안전성 |
| **빌드** | Vite | 빠른 HMR, 간결한 설정 |
| **UI 라이브러리** | shadcn/ui + Tailwind CSS | 커스터마이징 자유도, 경량 |
| **상태 관리** | TanStack Query | 서버 상태 캐싱, 자동 갱신 |
| **라우팅** | React Router v7 | SPA 라우팅 |
| **그래프 시각화** | React Flow | Task 의존성 그래프 렌더링 |
| **아이콘** | Lucide React | shadcn/ui 기본 아이콘셋 |
| **배포** | Go embed | 빌드 결과물을 claribot 바이너리에 내장 |

### 1.4 디렉토리 구조
```
claribot/
├── gui/                          # Web UI 소스코드
│   ├── src/
│   │   ├── components/           # 공통 UI 컴포넌트
│   │   │   ├── layout/           # Header, Sidebar, Layout
│   │   │   └── ui/               # shadcn/ui 컴포넌트
│   │   ├── pages/                # 페이지별 컴포넌트
│   │   │   ├── Dashboard.tsx
│   │   │   ├── Projects.tsx
│   │   │   ├── Tasks.tsx
│   │   │   ├── Messages.tsx
│   │   │   ├── Schedules.tsx
│   │   │   └── Settings.tsx
│   │   ├── hooks/                # 커스텀 훅
│   │   ├── api/                  # API 클라이언트
│   │   ├── types/                # TypeScript 타입
│   │   └── App.tsx
│   ├── package.json
│   ├── vite.config.ts
│   └── tsconfig.json
├── bot/
│   └── internal/
│       └── webui/                # Go embed + HTTP 핸들러
│           ├── webui.go          # embed.FS, 정적 파일 서빙
│           └── dist/             # 빌드 결과물 (gitignore)
```

---

## 2. API 연동 설계

### 2.1 기존 API 활용
현재 claribot은 `POST /` 단일 엔드포인트에 JSON command 구조로 통신한다. 웹UI도 동일한 API를 사용한다.

```typescript
// api/client.ts
interface ClaribotRequest {
  command: string;
  subcommand: string;
  args: string[];
  flags: Record<string, any>;
}

interface ClaribotResponse {
  success: boolean;
  message: string;
  data: any;
  needs_input: boolean;
  prompt: string;
  context: string;
}

async function execute(req: ClaribotRequest): Promise<ClaribotResponse> {
  const res = await fetch('/api', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(req),
  });
  return res.json();
}
```

### 2.2 웹UI 전용 엔드포인트 (추가 필요)

기존 API로 커버되지 않는 기능을 위해 최소한의 엔드포인트를 추가한다.

| 엔드포인트 | 메서드 | 용도 |
|-----------|--------|------|
| `/api` | POST | 기존 command 라우터 프록시 |
| `/api/stream` | WebSocket | Claude 실행 로그 실시간 스트리밍 |
| `/api/health` | GET | 서비스 헬스체크 (uptime, version) |

### 2.3 실시간 통신
Claude Code 실행 중 진행 상황을 실시간으로 표시하기 위해 WebSocket을 사용한다.

```
클라이언트 ──WebSocket──▶ /api/stream
                          │
                          ├─ claude:output   (Claude 실행 로그)
                          ├─ task:updated    (Task 상태 변경)
                          ├─ message:updated (Message 상태 변경)
                          └─ schedule:fired  (스케줄 실행 알림)
```

---

## 3. 페이지 구성

### 3.1 전체 레이아웃

```
┌──────────────────────────────────────────────────┐
│  Header: 로고 / 프로젝트 선택기 / 시스템 상태     │
├──────────┬───────────────────────────────────────┤
│          │                                       │
│ Sidebar  │           Main Content                │
│          │                                       │
│ 📊 대시보드│                                       │
│ 📁 프로젝트│                                       │
│ ✅ Tasks  │                                       │
│ 💬 메시지 │                                       │
│ ⏰ 스케줄 │                                       │
│ ⚙ 설정   │                                       │
│          │                                       │
└──────────┴───────────────────────────────────────┘
```

**Header 구성:**
- 좌측: Claribot 로고
- 중앙: 프로젝트 선택 드롭다운 (현재 선택된 프로젝트 표시, "전역" 옵션 포함)
- 우측: Claude 상태 인디케이터 (사용중/대기/오프라인), 시스템 상태 뱃지

**Sidebar 구성:**
- 네비게이션 메뉴 (아이콘 + 텍스트)
- 축소 모드 지원 (아이콘만 표시)
- 활성 메뉴 하이라이트

---

### 3.2 대시보드 (Dashboard)

**경로**: `/`

시스템 전체 현황을 한눈에 파악하는 메인 페이지.

```
┌─────────────────────────────────────────────────┐
│                    Dashboard                     │
├──────────┬──────────┬──────────┬────────────────┤
│ Claude   │ Tasks    │ Messages │ Schedules      │
│ ● 2/3    │ 12 total │ 3 진행중 │ 5 활성         │
│ 사용중    │ 4 대기   │ 47 완료  │ 다음: 07:00    │
└──────────┴──────────┴──────────┴────────────────┘
│                                                  │
│  [최근 활동 타임라인]                               │
│  ─ 14:23 Task #7 "API 엔드포인트 구현" 완료         │
│  ─ 14:20 Schedule #3 실행 시작                     │
│  ─ 14:15 Message "코드 리뷰해줘" 처리 완료          │
│  ─ 14:00 Task #5 "DB 스키마 설계" Plan 생성         │
│                                                  │
├──────────────────┬───────────────────────────────┤
│ Task 상태 분포    │  프로젝트별 Task 현황            │
│ [파이 차트]       │  [바 차트]                      │
│ todo: 4          │  claribot: 8                   │
│ planned: 3       │  blog: 3                       │
│ done: 12         │  api-server: 1                 │
│ failed: 1        │                               │
└──────────────────┴───────────────────────────────┘
```

**구성 요소:**
1. **요약 카드 4개**: Claude 사용량, Task 현황, Message 현황, Schedule 현황
2. **최근 활동 타임라인**: 전체 시스템의 최근 이벤트 (Task 완료, 메시지 처리, 스케줄 실행 등)
3. **Task 상태 분포 차트**: 현재 프로젝트의 Task 상태별 비율
4. **프로젝트별 활동 현황**: 프로젝트별 활성 Task 수

**데이터 갱신**: TanStack Query로 30초 간격 자동 폴링 + WebSocket 이벤트 수신 시 즉시 갱신

---

### 3.3 프로젝트 관리 (Projects)

**경로**: `/projects`

```
┌─────────────────────────────────────────────────┐
│  Projects                    [+ 프로젝트 추가]   │
├─────────────────────────────────────────────────┤
│                                                  │
│  ┌─────────────────────────────────────────┐    │
│  │ 📁 claribot          dev.platform       │    │
│  │ /mnt/c/Users/mail/git/claribot         │    │
│  │ LLM 기반 프로젝트 자동 실행 시스템         │    │
│  │ Tasks: 12 | 활성 스케줄: 3               │    │
│  │                     [선택] [편집] [삭제]  │    │
│  └─────────────────────────────────────────┘    │
│                                                  │
│  ┌─────────────────────────────────────────┐    │
│  │ 📁 blog              write.webnovel     │    │
│  │ /mnt/c/Users/mail/git/blog              │    │
│  │ 개인 블로그                               │    │
│  │ Tasks: 3 | 활성 스케줄: 1                │    │
│  └─────────────────────────────────────────┘    │
│                                                  │
└─────────────────────────────────────────────────┘
```

**기능:**
- 프로젝트 카드 리스트 (그리드/리스트 전환 가능)
- 카드에 프로젝트 ID, 타입, 경로, 설명, Task/Schedule 요약 표시
- `[선택]` 버튼: 해당 프로젝트로 전환 (Header의 드롭다운과 동기화)
- `[+ 프로젝트 추가]`: 모달 다이얼로그 (경로, 타입, 설명 입력)
- 타입 필터: `dev.platform`, `dev.cli`, `write.webnovel`

---

### 3.4 Task 관리 (Tasks) ⭐ 핵심 페이지

**경로**: `/tasks`

이 페이지가 Claribot 웹UI의 핵심이다. Task 트리 구조와 의존성 그래프를 시각적으로 관리한다.

#### 3.4.1 Task 트리 뷰 (기본 뷰)

```
┌────────────────────────────────────────────────────────────┐
│  Tasks (claribot)          [🌳 트리] [📊 그래프] [📋 리스트] │
│                            [+ Task 추가] [▶ Plan All] [▶ Run All] [🔄 Cycle] │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ▼ #1 API 서버 리팩토링              split         depth:0  │
│  │                                                         │
│  ├─ ▼ #2 라우터 모듈화               split         depth:1  │
│  │  ├── #4 핸들러 분리               ● planned     depth:2  │
│  │  └── #5 미들웨어 추가             ● planned     depth:2  │
│  │                                                         │
│  ├── #3 에러 핸들링 통합             ✅ done        depth:1  │
│  │                                                         │
│  └── #6 테스트 작성                  ○ todo        depth:1  │
│                                                            │
│  #7 문서 업데이트                    ○ todo        depth:0  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**트리 인터랙션:**
- 클릭: Task 상세 패널 열기 (우측 슬라이드)
- 더블클릭: 인라인 제목 편집
- 드래그: Task 순서 변경 (같은 부모 내에서)
- 노드 접기/펼치기

**상태 아이콘:**
- `○` todo (회색)
- `◐` split (파란색)
- `●` planned (노란색)
- `✅` done (녹색)
- `❌` failed (빨간색)

#### 3.4.2 의존성 그래프 뷰

```
┌─────────────────────────────────────────────────┐
│  [🌳 트리] [📊 그래프] [📋 리스트]                 │
├─────────────────────────────────────────────────┤
│                                                  │
│     ┌────┐         ┌────┐                       │
│     │ #4 │────────▶│ #5 │                       │
│     └────┘         └────┘                       │
│        │                │                       │
│        ▼                ▼                       │
│     ┌────┐         ┌────┐                       │
│     │ #3 │────────▶│ #6 │                       │
│     └────┘         └────┘                       │
│                                                  │
│  [+ Edge 추가]  노드를 드래그하여 연결             │
│                                                  │
└─────────────────────────────────────────────────┘
```

**React Flow 기반 그래프:**
- 노드: Task (상태별 색상)
- 엣지: 의존성 (화살표 = 선행→후행)
- 드래그로 노드 연결하여 Edge 추가
- 엣지 클릭으로 삭제
- 자동 레이아웃 (dagre 알고리즘)
- 줌/팬 지원

#### 3.4.3 리스트 뷰

```
┌──────────────────────────────────────────────────────────┐
│  [🌳 트리] [📊 그래프] [📋 리스트]                         │
│  필터: [전체 ▼] [상태: 전체 ▼] [Leaf만 □]                 │
├────┬───────────────────┬────────────┬───────┬────────────┤
│ ID │ 제목               │ 상태       │ Depth │ 부모       │
├────┼───────────────────┼────────────┼───────┼────────────┤
│ 1  │ API 서버 리팩토링   │ split      │ 0     │ -          │
│ 2  │ 라우터 모듈화       │ split      │ 1     │ #1         │
│ 3  │ 에러 핸들링 통합    │ done       │ 1     │ #1         │
│ 4  │ 핸들러 분리        │ planned    │ 2     │ #2         │
│ 5  │ 미들웨어 추가      │ planned    │ 2     │ #2         │
│ 6  │ 테스트 작성        │ todo       │ 1     │ #1         │
│ 7  │ 문서 업데이트      │ todo       │ 0     │ -          │
└────┴───────────────────┴────────────┴───────┴────────────┘
```

**필터링:**
- 상태별: todo, split, planned, done, failed
- Leaf만 보기 (is_leaf = true)
- 부모 Task별 필터링

#### 3.4.4 Task 상세 패널 (우측 슬라이드)

```
┌───────────────────────────────┐
│  Task #4                  [×] │
│  핸들러 분리                    │
├───────────────────────────────┤
│                               │
│  상태: ● planned              │
│  부모: #2 라우터 모듈화        │
│  Depth: 2                     │
│  Leaf: Yes                    │
│  생성: 2025-01-15 14:00       │
│  수정: 2025-01-15 15:30       │
│                               │
│  ── Spec ──────────────────── │
│  HTTP 핸들러를 기능별로 분리하여│
│  각각의 파일로 나눈다.         │
│  - project 핸들러             │
│  - task 핸들러                │
│  - message 핸들러             │
│  [편집]                       │
│                               │
│  ── Plan ──────────────────── │
│  ## 구현 방향                  │
│  1. handler/router.go에서     │
│     각 명령어 핸들러 추출       │
│  2. handler/project.go 생성   │
│  3. handler/task.go 생성      │
│  [편집]                       │
│                               │
│  ── Report ────────────────── │
│  (실행 전)                     │
│                               │
│  ── Dependencies ──────────── │
│  선행: #3 에러 핸들링 통합     │
│  후행: #5 미들웨어 추가        │
│  [+ Edge 추가]                │
│                               │
│  ─────────────────────────── │
│  [▶ Plan] [▶ Run] [🗑 삭제]  │
│                               │
└───────────────────────────────┘
```

**기능:**
- Spec, Plan, Report를 마크다운으로 렌더링
- 각 필드 인라인 편집 (마크다운 에디터)
- 상태 변경 드롭다운
- 의존성(Edge) 관리
- Plan/Run 버튼으로 개별 실행

#### 3.4.5 Task 액션 버튼

| 버튼 | 동작 | API 호출 |
|------|------|---------|
| `+ Task 추가` | 새 Task 생성 모달 (제목, 부모, Spec 입력) | `task add` |
| `▶ Plan All` | 전체 todo Task 계획 수립 | `task plan --all` |
| `▶ Run All` | 전체 planned leaf Task 실행 | `task run --all` |
| `🔄 Cycle` | Plan + Run 자동 순회 | `task cycle` |
| `▶ Plan` (개별) | 단일 Task 계획 수립 | `task plan <id>` |
| `▶ Run` (개별) | 단일 Task 실행 | `task run <id>` |

---

### 3.5 메시지 (Messages)

**경로**: `/messages`

```
┌─────────────────────────────────────────────────────────┐
│  Messages (claribot)                                     │
│  [상태 필터: 전체 ▼]                                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─ 메시지 입력 ──────────────────────────────────────┐  │
│  │                                                    │  │
│  │  Claude에게 보낼 메시지를 입력하세요...               │  │
│  │                                            [전송]  │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  ── 메시지 히스토리 ──────────────────────────────────── │
│                                                          │
│  📨 #47  "API 엔드포인트 목록 정리해줘"                    │
│  📍 cli | ✅ done | 2025-01-15 14:23                     │
│  ▼ 결과 보기                                             │
│  ┌──────────────────────────────────────────────────┐   │
│  │ ## API 엔드포인트 목록                              │   │
│  │ 1. POST /api - 커맨드 실행                         │   │
│  │ 2. GET /api/health - 헬스체크                      │   │
│  │ ...                                               │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  📨 #46  "코드 리뷰해줘"                                 │
│  📍 telegram | 🔄 processing | 2025-01-15 14:20          │
│  ▼ 실시간 로그                                           │
│  ┌──────────────────────────────────────────────────┐   │
│  │ [실시간 Claude 출력 스트리밍...]                     │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  📨 #45  "테스트 실행해줘"                                │
│  📍 schedule | ✅ done | 2025-01-15 14:00                │
│                                                          │
│  [더 보기]                                               │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**기능:**
- 상단 메시지 입력: Claude에게 직접 메시지 전송 (`message send cli <text>`)
- 메시지 히스토리: 최신순 정렬, 무한 스크롤
- 상태 필터: pending, processing, done, failed
- source 아이콘: telegram, cli, schedule
- 결과 접기/펼치기 (마크다운 렌더링)
- processing 상태 메시지: WebSocket으로 실시간 로그 스트리밍

---

### 3.6 스케줄 (Schedules)

**경로**: `/schedules`

```
┌─────────────────────────────────────────────────────────┐
│  Schedules                          [+ 스케줄 추가]      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌───────────────────────────────────────────────────┐  │
│  │ #1  매일 코드 리뷰                          [ON]  │  │
│  │ ⏰ 0 9 * * 1-5  (평일 09:00)                      │  │
│  │ 📁 claribot                                       │  │
│  │ 💬 "코드 변경사항 리뷰하고 이슈 정리해줘"            │  │
│  │ 마지막 실행: 2025-01-15 09:00 ✅                   │  │
│  │ 다음 실행: 2025-01-16 09:00                       │  │
│  │                        [실행 기록] [편집] [삭제]    │  │
│  └───────────────────────────────────────────────────┘  │
│                                                          │
│  ┌───────────────────────────────────────────────────┐  │
│  │ #2  일일 보고서                            [ON]   │  │
│  │ ⏰ 0 18 * * *  (매일 18:00)                       │  │
│  │ 📁 (전역)                                         │  │
│  │ 💬 "오늘 진행된 작업 요약 보고서 작성해줘"            │  │
│  │ 마지막 실행: 2025-01-15 18:00 ✅                   │  │
│  │ 다음 실행: 2025-01-16 18:00                       │  │
│  └───────────────────────────────────────────────────┘  │
│                                                          │
│  ┌───────────────────────────────────────────────────┐  │
│  │ #3  일회성 알림                            [OFF]  │  │
│  │ ⏰ 30 14 * * *  (1회 실행 완료)                    │  │
│  │ 📁 (전역)                                         │  │
│  │ 💬 "알림 테스트"                                   │  │
│  │ run_once: true | 비활성화됨                        │  │
│  └───────────────────────────────────────────────────┘  │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**스케줄 추가 모달:**
```
┌─────────────────────────────────┐
│  새 스케줄 추가              [×] │
├─────────────────────────────────┤
│                                 │
│  Cron 표현식:                    │
│  ┌─────────────────────────┐   │
│  │ 0 9 * * 1-5             │   │
│  └─────────────────────────┘   │
│  → 평일 09:00 (읽기 쉬운 설명)   │
│                                 │
│  메시지:                         │
│  ┌─────────────────────────┐   │
│  │ 코드 리뷰해줘             │   │
│  └─────────────────────────┘   │
│                                 │
│  프로젝트:                       │
│  [claribot ▼]                   │
│                                 │
│  ☐ 1회만 실행 (run_once)        │
│                                 │
│              [취소] [추가]       │
└─────────────────────────────────┘
```

**실행 기록 뷰 (모달 또는 하위 페이지):**
```
┌─────────────────────────────────────────────┐
│  Schedule #1 실행 기록                   [×] │
├──────┬──────────┬────────┬──────────────────┤
│ Run  │ 시작     │ 상태   │ 소요 시간         │
├──────┼──────────┼────────┼──────────────────┤
│ #15  │ 01-15 09:00 │ ✅ done │ 2분 34초    │
│ #14  │ 01-14 09:00 │ ✅ done │ 1분 50초    │
│ #13  │ 01-13 09:00 │ ❌ failed│ 0분 12초   │
│ #12  │ 01-10 09:00 │ ✅ done │ 3분 10초    │
└──────┴──────────┴────────┴──────────────────┘
│                                              │
│ Run #15 상세:                                │
│ ┌──────────────────────────────────────┐    │
│ │ ## 코드 리뷰 결과                      │    │
│ │ 1. handler/router.go - 에러 처리 개선  │    │
│ │ 2. task/plan.go - 주석 추가            │    │
│ │ ...                                   │    │
│ └──────────────────────────────────────┘    │
└──────────────────────────────────────────────┘
```

**기능:**
- 스케줄 카드 리스트
- ON/OFF 토글 스위치 (`schedule enable/disable`)
- Cron 표현식을 사람이 읽기 쉬운 형태로 변환하여 표시
- 실행 기록 조회 및 결과 확인
- 프로젝트별 필터링

---

### 3.7 설정 (Settings)

**경로**: `/settings`

```
┌─────────────────────────────────────────────────┐
│  Settings                                        │
├─────────────────────────────────────────────────┤
│                                                  │
│  ── 시스템 정보 ───────────────────────────────── │
│  Claribot 버전: v0.2.20                          │
│  CLI 버전: v0.2.5                                │
│  가동 시간: 3일 14시간 22분                        │
│  DB 경로: ~/.claribot/db.clt                     │
│                                                  │
│  ── Claude Code ──────────────────────────────── │
│  최대 동시 실행: 3                                │
│  현재 사용중: 1                                   │
│  유휴 타임아웃: 1200초                             │
│                                                  │
│  ── 활성 세션 ────────────────────────────────── │
│  #1: claribot (task plan) - 2분 30초             │
│  #2: (유휴)                                      │
│  #3: (유휴)                                      │
│                                                  │
│  ── 연결 상태 ────────────────────────────────── │
│  Service: ● 연결됨 (127.0.0.1:9847)             │
│  Telegram: ● 연결됨                              │
│  WebSocket: ● 연결됨                             │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

## 4. 공통 UI 컴포넌트

### 4.1 Claude 실행 모달

Claude Code가 실행 중일 때 표시되는 모달/토스트:

```
┌─────────────────────────────────────┐
│  🔄 Claude 실행 중...            [×] │
│                                     │
│  Task #4 "핸들러 분리" 실행 중        │
│  경과: 1분 24초                      │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ > handler/router.go 분석 중  │   │
│  │ > project 핸들러 분리 작업... │   │
│  │ > handler/project.go 생성   │   │
│  │ > ...                       │   │
│  │ [실시간 로그 스트리밍]        │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

### 4.2 확인 다이얼로그

삭제 등 위험한 작업 시 표시:

```
┌──────────────────────────────┐
│  ⚠ 삭제 확인                  │
│                              │
│  Task #4 "핸들러 분리"를       │
│  삭제하시겠습니까?             │
│                              │
│  이 작업은 되돌릴 수 없습니다.  │
│                              │
│         [취소] [삭제]         │
└──────────────────────────────┘
```

### 4.3 토스트 알림

```
┌──────────────────────────────┐
│ ✅ Task #4 Plan 생성 완료     │  ← 성공 (녹색, 3초 후 자동 닫힘)
└──────────────────────────────┘

┌──────────────────────────────┐
│ ❌ 스케줄 추가 실패: 잘못된    │  ← 에러 (빨간색, 수동 닫힘)
│    cron 표현식               │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 🔔 Schedule #1 실행 완료     │  ← WebSocket 알림 (파란색)
└──────────────────────────────┘
```

---

## 5. 배포 및 통합

### 5.1 Go embed 통합

웹UI 빌드 결과물을 Go 바이너리에 내장하여 별도 웹서버 없이 동작한다.

```go
// bot/internal/webui/webui.go
package webui

import "embed"

//go:embed dist/*
var staticFiles embed.FS
```

**빌드 흐름:**
```
gui/ → npm run build → gui/dist/ → cp → bot/internal/webui/dist/ → go build (embed) → claribot 바이너리
```

### 5.2 HTTP 라우팅

```go
// 기존 claribot HTTP 서버에 추가
mux.HandleFunc("/api", handleAPI)          // 기존 command 라우터
mux.HandleFunc("/api/stream", handleWS)    // WebSocket
mux.HandleFunc("/api/health", handleHealth)
mux.Handle("/", webui.Handler())           // 정적 파일 서빙 (SPA fallback)
```

**SPA Fallback**: `/api`로 시작하지 않는 모든 요청은 `index.html`로 리다이렉트 (React Router 지원).

### 5.3 Makefile 추가

```makefile
build-gui:
	cd gui && npm install && npm run build
	rm -rf bot/internal/webui/dist
	cp -r gui/dist bot/internal/webui/dist

build: build-gui build-cli build-bot

dev-gui:
	cd gui && npm run dev
```

### 5.4 설정

```yaml
# config.yaml 추가
webui:
  enabled: true       # 웹UI 활성화/비활성화
  port: 9848          # 웹UI 포트 (별도) 또는 기존 포트 공유
```

---

## 6. 구현 우선순위

### Phase 1: 기반 (MVP) ✅
1. ~~프로젝트 scaffolding (Vite + React + TypeScript + shadcn/ui)~~ ✅
2. ~~API 클라이언트 모듈 (`/api` POST 통신)~~ ✅
3. ~~Layout 컴포넌트 (Header + Sidebar + Main)~~ ✅
4. ~~프로젝트 선택기 (드롭다운)~~ ✅
5. ~~Dashboard 페이지 (요약 카드 4개)~~ ✅
6. ~~Go embed 통합 및 정적 파일 서빙~~ ✅

### Phase 2: 핵심 기능 ✅
7. ~~Projects 페이지 (CRUD)~~ ✅
8. ~~Tasks 페이지 - 리스트 뷰~~ ✅
9. ~~Tasks 페이지 - 트리 뷰~~ ✅
10. ~~Task 상세 패널 (Spec/Plan/Report 표시 및 편집)~~ ✅
11. ~~Task 실행 버튼 (Plan/Run/Cycle)~~ ✅
12. ~~Messages 페이지 (히스토리 + 전송)~~ ✅

### Phase 3: 시각화
13. Task 의존성 그래프 뷰 (React Flow)
14. Edge 관리 (드래그로 연결/삭제)
15. ~~Dashboard 차트 (Task 상태 분포)~~ ✅

### Phase 4: 실시간
16. WebSocket 연동 (`/api/stream`)
17. Claude 실행 로그 실시간 스트리밍
18. Task/Message 상태 실시간 갱신
19. 토스트 알림 (스케줄 실행 완료 등)

### Phase 5: 완성도 ✅ (부분)
20. ~~Schedules 페이지 (CRUD + 실행 기록)~~ ✅
21. ~~Settings 페이지~~ ✅
22. 다크 모드 지원 (CSS 변수 준비완료, 토글 미구현)
23. 반응형 레이아웃 (모바일 대응)
24. 키보드 단축키 (Task 탐색, 실행)

---

## 7. 화면 흐름도

```
[Dashboard] ──── 프로젝트 선택 ────▶ [Dashboard 갱신]
     │
     ├──▶ [Projects] ── 프로젝트 선택 ──▶ [Tasks]
     │
     ├──▶ [Tasks]
     │      ├── 트리 뷰 ── Task 클릭 ──▶ [Task 상세 패널]
     │      ├── 그래프 뷰 ── 노드 드래그 ──▶ [Edge 생성]
     │      ├── 리스트 뷰 ── 행 클릭 ──▶ [Task 상세 패널]
     │      └── Plan/Run/Cycle 버튼 ──▶ [Claude 실행 모달]
     │
     ├──▶ [Messages]
     │      ├── 메시지 전송 ──▶ [Claude 실행 모달]
     │      └── 결과 접기/펼치기
     │
     ├──▶ [Schedules]
     │      ├── 스케줄 추가 ──▶ [추가 모달]
     │      ├── ON/OFF 토글
     │      └── 실행 기록 ──▶ [실행 기록 뷰]
     │
     └──▶ [Settings]
```
