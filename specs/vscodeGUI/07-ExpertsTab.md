# VSCode Extension Experts 탭

> **버전**: v0.0.4

## Experts 탭 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│  [Project]  [Features]  [Tasks]  [Experts]              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Assigned Experts (2)                                   │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 🔧 backend-go-gin                          [Edit]  │ │
│  │    Backend Go GIN Developer                        │ │
│  │ ┌────────────────────────────────────────────────┐ │ │
│  │ │ ## Role Definition                             │ │ │
│  │ │ Go 언어와 GIN 프레임워크를 사용한...            │ │ │
│  │ │                                                │ │ │
│  │ │ ## Tech Stack                                  │ │ │
│  │ │ - **Language**: Go 1.21+                       │ │ │
│  │ │ - **Framework**: GIN v1.9+                     │ │ │
│  │ │ ...                                            │ │ │
│  │ └────────────────────────────────────────────────┘ │ │
│  │                                        [Unassign]  │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 🎨 frontend-react                          [Edit]  │ │
│  │    Frontend React Developer                        │ │
│  │ ┌────────────────────────────────────────────────┐ │ │
│  │ │ (렌더링된 마크다운 내용)                        │ │ │
│  │ └────────────────────────────────────────────────┘ │ │
│  │                                        [Unassign]  │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  ─────────────────────────────────────────────────────  │
│                                                         │
│  Available Experts (1)                                  │
│  ┌────────────────────────────────────────────────────┐ │
│  │ ☁️ devops-k8s                              [Assign] │ │
│  │    DevOps Kubernetes Specialist                    │ │
│  └────────────────────────────────────────────────────┘ │
│                                                         │
│  [+ Create New Expert]                                  │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  Status: Connected │ Last sync: 2s ago │ WAL mode: ON   │
└─────────────────────────────────────────────────────────┘
```

## Experts 탭 기능

| 기능 | 설명 |
|------|------|
| **마크다운 렌더링** | EXPERT.md 내용을 렌더링된 형태로 표시 (react-markdown) |
| **[Edit] 버튼** | VSCode 기본 에디터로 EXPERT.md 파일 열기 |
| **[Assign] 버튼** | Expert를 현재 프로젝트에 할당 |
| **[Unassign] 버튼** | Expert를 현재 프로젝트에서 해제 |
| **[+ Create New Expert]** | 새 Expert 생성 (ID 입력 → 템플릿 생성 → 파일 열기) |

---

## Experts 탭 컴포넌트 구조

```
ExpertsPanel/
├── ExpertsPanel.tsx       # Experts 탭 메인
├── ExpertCard.tsx         # Expert 카드 (렌더링 + 버튼)
├── ExpertContent.tsx      # 마크다운 렌더링 영역
└── CreateExpertDialog.tsx # 새 Expert 생성 다이얼로그
```

---

## Expert 데이터 타입

```typescript
interface Expert {
  id: string;
  name: string;
  version: string;
  domain: string;
  language: string;
  framework: string;
  content: string;       // EXPERT.md 전체 내용
  assigned: boolean;
  fileExists: boolean;   // 파일 존재 여부
}
```

---

*Claritask VSCode Extension Spec v0.0.4 - 2026-02-03*
